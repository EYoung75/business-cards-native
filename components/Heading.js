import React from "react"
import { Header, H1 } from "native-base"

const Heading = () => {
    return (
        <Header style={{width: "100%", flex: 1, justifyContent: "center"}}>
            <H1>Contax</H1>
        </Header>
    )
}

export default Heading